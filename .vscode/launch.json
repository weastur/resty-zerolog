{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run example",
      "type": "go",
      "request": "launch",
      "mode": "exec",
      "program": "${workspaceFolder}/_example/example",
      "args": [
        "ensure"
      ],
      "preLaunchTask": "build"
    }
  ]
}
